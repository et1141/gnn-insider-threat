[project]
name = "gnn-insider-threat"
version = "0.1.0"
description = "Deep Learning project for Insider Threat Detection using GCN and Bi-LSTM"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pandas",
    "numpy",
    "torch",
    "torch_geometric",
    "pytorch-lightning",
    "scikit-learn",
    "tqdm",
    "pyyaml",
    "requests",
    "matplotlib",
    "jupyter",
    "dvc[gdrive]",
    "wandb>=0.25.1",
    "torchvision>=0.26.0",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "ruff",
]
experiment = [
    "wandb",
]

[project.scripts]
preprocess = "certgnn.preprocess:main"
download-data = "certgnn.download_data:download_dataset"
train-baseline = "certgnn.baseline.train:main"
test-baseline = "certgnn.baseline.test_baseline:main"

[[tool.uv.index]]
name = "pytorch"
url = "https://download.pytorch.org/whl/cu126"
explicit = true

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/certgnn"]
